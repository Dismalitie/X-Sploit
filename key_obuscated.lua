local a = "LpDmXEmIN4sNsyltLoja"
return a
--[[scrypt: key = pull:from%string(local{in.project} math.random(specify?integer))
      if workspace:a?input == "LpDmXEmIN4sNsyltLoja" do
        print("u read the code. but you still got it wrong lol")
      end
      lol = find:{in.project} lolvariable
      if {in.current} not find:({scrypt.tag}) do
        print("u thought removing scrypt would work? try harder")
      end
return scrypt
--]]
